/**
 * Evaluates result permissions against provided schema
 * @param {Array<String>} roles 
 * @param {Object} securitySchema 
 * @returns {Object} result permissions
 */
export const getPermissions = (roles, securitySchema) => {

};